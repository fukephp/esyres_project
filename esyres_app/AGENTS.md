# Esyres app

See the git root [`AGENTS.md`](../AGENTS.md) and [`.cursor/CONTEXT.md`](../.cursor/CONTEXT.md).

Commands and verify run from this folder via slim Docker Compose (`php` + `vite` + `mysql`): `up -d` then `exec -T`. Product PWA is `frontend/`. Marketing is `marketing/`. Backend gate is Behat, not `php artisan test`.
