default:
  autoload:
    '': '%paths.base%/features/bootstrap'
  suites:
    owner:
      paths:
        - '%paths.base%/features/owner'
      contexts:
        - OwnerContext
    guest:
      paths:
        - '%paths.base%/features/guest'
      contexts:
        - GuestContext
