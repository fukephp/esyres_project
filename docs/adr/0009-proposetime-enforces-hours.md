# proposeTime enforces salon hours; acceptPreferredTime does not

The weekday open interval minus break still bounds `proposeTime` (`OUTSIDE_HOURS`). Droppable cells are gone (see `docs/adr/0035-zahtjevi-month-and-selected-day-list.md`); Request Detail submit is the path. `acceptPreferredTime` stays overlap-only — the owner’s Prihvati tap is still the assertion. Holidays are not in the app yet and are not checked here.
