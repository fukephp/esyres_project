# Public salon register; invite-only is revoked

Homepage “Get your panel” is a real owner door, not invite copy. It goes to `/owner`. Logged out: login/register there (register allowed). Unverified: existing email panel. No salon: create-salon form. Already owns one: panel, and they may create another from the panel. Same `users` row; customer `/register` does not create a salon. `createSalon` requires verified email. Input this story: name, address, lat, lng (entry method still grilled). New salon defaults stay closed week / empty catalog. Listing is ADR 0027. Rejected: founder provision as the only path, Formspree waitlist, a role toggle on `/register`.
