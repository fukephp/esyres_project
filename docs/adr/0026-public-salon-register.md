# Public salon register; invite-only is revoked

Homepage “Get your panel” is a real owner door, not invite copy. A signed-in user may create a salon; the same `users` row can be customer and owner. Rejected: keeping founder provision as the only way to own a salon, and a Formspree waitlist as the CTA. Customer `/register` stays public user signup. Email verify still gates `/owner`. Listing rules and create-form fields are the same grill, not this paragraph.
