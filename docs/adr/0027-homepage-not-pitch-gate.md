# `/` is the homepage; discovery lives at `/salons`

The one-shot company pitch (persist “seen”, then discovery on the same `/`) is gone. `/` is a permanent homepage: top-nav homepage slot, the existing hero, a simple footer. Pronađi salon goes to `/salons`. Auth does not skip the homepage; visiting `/` never auto-jumps to `/owner`. `/salon/:id` and `GET /qr/{id}` never show the homepage page. Shared top-nav on other routes is `docs/adr/0029-shared-top-nav.md`. Rejected: persist-seen on `/`, a sibling marketing site, `/welcome`, and a long-scroll feature-grid landing. This supersedes the same-URL pitch routing in `docs/adr/0024-company-pitch-in-pwa.md`.
