# Day-first salon booking chrome

Idle `Pošalji zahtjev` on load (under the title / sticky sidebar) competed with Radno vrijeme. Guests now pick an open weekday first; the pill sits under that selected hours row (later weekdays push down) and opens a picker **modal** (salon name + muted weekday+date). The `md+` aside stays an empty sticky gutter. Logged-in `/` adds Moje rezervacije between name and Odjava so bookings are reachable from the homepage. Pitaj salon opens a picker-like native dialog overlay (empty Cal greeting, unbubbled salon lines, light-gray guest pills, dummy composer), not an on-page thread, not a second black primary, and not LLM NLU. Rejected: instant modal on day tap; inline picker scroll; filling the aside; a second send-icon under the row. STORY-65 supersedes STORY-62 on pill-after-list and modal title-only. STORY-63 supersedes STORY-62 on-page messenger chrome. STORY-62 supersedes STORY-43/47/59 chrome ACs. See `docs/adr/0029-shared-top-nav.md` (homepage slot still not cloned onto `/salon/:id`).
