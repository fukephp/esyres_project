Attended sequential drain: print what-next, then immediately Local story-loop for Recommended. One PR at a time; wait for merge + continue.

Follow `.cursor/skills/start-building-stories/SKILL.md` exactly. That file is the source of truth.

Do not invent stories. Do not write `docs/stories/`. Do not run an unattended gauntlet. Refuse Cloud / `unattended` inside this drain. Named `/story-loop STORY-xx` (including `unattended`) stays available outside it.

If the user named this command with no extra story id, start the drain. Continue phrases in the same chat: continue / merged / next.
