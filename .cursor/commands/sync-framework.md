Propagate allowlisted Cursor defaults from esy_framework into one sibling project.

Follow `.cursor/skills/sync-framework/SKILL.md` exactly. That skill is the source of truth.

If the user named a project after this command (e.g. `/sync-framework esyres_project`), use that as the folder name. Otherwise ask for the name before copying.

Overwrite allowlisted paths only; print a short summary. Do not touch CONTEXT, product docs, story maps/keys, refs, app code, or target root AGENTS/README.
