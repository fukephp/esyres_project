---
description: Read DESIGN.md index, then the correct design pack — same Cal tokens, separate packs by surface
globs: esyres_app/frontend/**
alwaysApply: false
---

# Design system (DESIGN.md)

Before generating or changing visual UI:

1. Read root `DESIGN.md` (index — two packs, shared Cal system; do not mix company-pitch IA with product chrome).
2. Open the matching source:
   - **Company pitch** on typed `/` → `refs/design-1/DESIGN.md`
   - **Discovery / salon / owner** → `refs/design-2/DESIGN.md` (also `panel-ref.jpg` for owner skeleton), then `docs/mvp/04-UI-Design-Goals.md` and `.cursor/rules/frontend/` for product UX

- Design 1 is locked (Cal-based clean SaaS — white canvas, black CTAs, Cal Sans + Inter; MVP is one screen, not a long-scroll site). Design 2 is locked (same Cal tokens + role composition; owner dark nav; not a dashboard IA clone).
- Product UX still comes from `docs/mvp/` and `.cursor/rules/frontend/`.
