---
description: Always follow .cursor/CONTEXT.md across rules, skills, commands, and hooks
alwaysApply: true
---

# Follow project context

Read and obey `.cursor/CONTEXT.md` for every task. It applies to all sub-folders under `.cursor/` (`rules/`, `hooks/`, `skills/`, `commands/`).

- Product truth is `docs/mvp/`, not guesses.
- Architecture truth is `docs/architecture/`. Do not invent a stack.
- Stories inventory is `docs/stories/` (`STORY-xx.md`). Do not invent stories.
- Use `rules/frontend/` when touching UI. Use `rules/backend/` when touching APIs or data.
- Use skills and slash commands listed in CONTEXT.md instead of inventing a parallel workflow.
- Prefer **grilling** before locking a plan or design. User starts `/grill-me` when there is no app code; `/grill-with-docs` when a codebase exists to align against.
